CMD Prompt

```
cd <PATH>	change directory
.		current dir
..		parent dir

mkdir		makes a directory
```
Git Notes

```
git init	intitalize git
git status 	Whats going on?
git add <filename> add one file to stage
git add -A 	Adds All
git commit -m "msg" Commit
git push orgin master	push to master

```